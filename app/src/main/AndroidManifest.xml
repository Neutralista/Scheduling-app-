<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!--
        Usage Access — special permission granted manually via Settings → Apps →
        Special app access → Usage access. Not a runtime dialog; the app redirects
        the user to Settings when a widget needs it.
    -->
    <uses-permission
        android:name="android.permission.PACKAGE_USAGE_STATS"
        tools:ignore="ProtectedPermissions" />

    <!--
        Health Connect — data-type permissions (e.g. READ_STEPS, WRITE_STEPS) are
        added here as widgets that need them are built. The <queries> entry is
        required so the app can detect whether Health Connect is installed.
    -->
    <uses-permission android:name="android.permission.READ_CALENDAR" />
    <uses-permission android:name="android.permission.WRITE_CALENDAR" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.health.READ_STEPS" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK" />
    <uses-permission android:name="android.permission.USE_FULL_SCREEN_INTENT" />
    <uses-permission android:name="android.permission.VIBRATE" />

    <queries>
        <!-- Standalone Health Connect app (Android 9–13) -->
        <package android:name="com.google.android.apps.healthdata" />
        <!--
            On Android 14+ Health Connect is built into the OS under a different
            component name. Querying by intent action covers both variants so
            Android doesn't block the permission request intent.
        -->
        <intent>
            <action android:name="androidx.health.connect.client.action.REQUEST_HEALTH_PERMISSIONS" />
        </intent>

    </queries>

    <application
        android:name=".WaypointApplication"
        android:label="Waypoint"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:allowBackup="false"
        android:theme="@style/Theme.Waypoint">

        <receiver
            android:name=".notification.BootReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
            </intent-filter>
        </receiver>

        <receiver
            android:name=".notification.ShiftEndAlarmReceiver"
            android:exported="false" />

        <service
            android:name="androidx.work.impl.foreground.SystemForegroundService"
            android:foregroundServiceType="shortService"
            tools:node="merge" />

        <receiver
            android:name=".notification.ScriptNotificationReceiver"
            android:exported="false" />

        <receiver
            android:name=".notification.ScriptNotificationActionReceiver"
            android:exported="false" />

        <receiver
            android:name=".planner.SleepCheckReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="com.waypoint.app.SLEEP_CHECK" />
            </intent-filter>
        </receiver>

        <receiver
            android:name=".notification.SleepAlarmReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="com.waypoint.app.SLEEP_PRE_REMINDER" />
                <action android:name="com.waypoint.app.SLEEP_BEDTIME" />
                <action android:name="com.waypoint.app.SLEEP_LATE_NUDGE" />
            </intent-filter>
        </receiver>

        <receiver
            android:name=".notification.SleepActionReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="com.waypoint.app.ENTER_SLEEP_MODE" />
            </intent-filter>
        </receiver>

        <receiver
            android:name=".notification.WakeAlarmReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="com.waypoint.app.WAKE_GENTLE" />
                <action android:name="com.waypoint.app.WAKE_MEDIUM" />
                <action android:name="com.waypoint.app.WAKE_RING" />
            </intent-filter>
        </receiver>

        <receiver
            android:name=".alarm.UserAlarmReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="com.waypoint.app.USER_ALARM_FIRE" />
            </intent-filter>
        </receiver>

        <activity
            android:name=".notification.AlarmRingActivity"
            android:exported="false"
            android:showOnLockScreen="true"
            android:turnScreenOn="true"
            android:theme="@style/Theme.Waypoint" />

        <service
            android:name=".notification.AlarmRingService"
            android:exported="false"
            android:foregroundServiceType="mediaPlayback" />

        <!--
            Health Connect — rationale activity.
            The action differs by HC variant:
              - Standalone HC app (Android 9–13): androidx.health.ACTION_SHOW_PERMISSIONS_RATIONALE
              - Platform HC (Android 14+):        android.intent.action.VIEW_PERMISSION_USAGE
                                                  via the activity-alias below
        -->
        <activity
            android:name=".permission.PermissionRationaleActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="androidx.health.ACTION_SHOW_PERMISSIONS_RATIONALE" />
            </intent-filter>
        </activity>

        <!--
            Required for platform Health Connect on Android 14+.
            Without this alias the permission dialog is silently rejected on
            devices where HC is baked into the OS instead of a standalone app.
        -->
        <activity-alias
            android:name="ViewPermissionUsageActivity"
            android:exported="true"
            android:targetActivity=".permission.PermissionRationaleActivity"
            android:permission="android.permission.START_VIEW_PERMISSION_USAGE">
            <intent-filter>
                <action android:name="android.intent.action.VIEW_PERMISSION_USAGE" />
                <category android:name="android.intent.category.HEALTH_PERMISSIONS" />
            </intent-filter>
        </activity-alias>

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>

</manifest>
