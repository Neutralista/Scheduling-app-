<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!--
        Usage Access — special permission granted manually via Settings → Apps →
        Special app access → Usage access. Not a runtime dialog; the app redirects
        the user to Settings when a widget needs it.
    -->
    <uses-permission
        android:name="android.permission.PACKAGE_USAGE_STATS"
        tools:ignore="ProtectedPermissions" />

    <!--
        Health Connect — data-type permissions (e.g. READ_STEPS, WRITE_STEPS) are
        added here as widgets that need them are built. The <queries> entry is
        required so the app can detect whether Health Connect is installed.
    -->
    <uses-permission android:name="android.permission.READ_CALENDAR" />
    <uses-permission android:name="android.permission.health.READ_STEPS" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />

    <queries>
        <package android:name="com.google.android.apps.healthdata" />
    </queries>

    <application
        android:name=".WaypointApplication"
        android:label="Waypoint"
        android:allowBackup="false"
        android:theme="@style/Theme.Waypoint">

        <receiver
            android:name=".notification.ShiftEndAlarmReceiver"
            android:exported="false" />

        <!--
            Required by Health Connect: without this activity + intent filter,
            the HC permission request is silently rejected on all Android versions.
        -->
        <activity
            android:name=".permission.PermissionRationaleActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="androidx.health.connect.client.SHOW_PERMISSIONS_RATIONALE" />
            </intent-filter>
        </activity>

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>

</manifest>
